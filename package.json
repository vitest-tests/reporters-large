{
  "name": "reporters-large",
  "private": true,
  "type": "module",
  "scripts": {
    "postinstall": "node ./generate-files.mjs",
    "cpu-profile": "node ./run-tests.mjs",
    "test": "vitest run ./tests/**"
  },
  "devDependencies": {
    "@types/node": "^20.10.4",
    "node-pty": "^1.0.0",
    "vitest": "^1.0.4"
  }
}
